# {{ ansible_managed }}

# CAUTION: Any syntax error introduced here will break sudo.

# Allow dedicated user account to power-off the machine
{{ umc_user_shutdown_add_name }} ALL=(root) NOPASSWD: /usr/bin/systemctl --ignore-inhibitors --no-wall poweroff
