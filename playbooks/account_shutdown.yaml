- name: Ubuntu Maintenance - Add dedicated shutdown account
  gather_facts: true
  hosts: all

  tasks:
    - name: Run account_shutdown role
      ansible.builtin.include_role:
        name: account_shutdown
